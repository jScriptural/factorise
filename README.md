# FACTORISE
This program uses Fermat method to find the f prime factors of a number.

_**Caveat**_: if given a decimal number, the fractional part is ignored.
```$ factorise 34.12```
will yield only the factors of 34. The output is like below.
```34: 2 17```.
Also when given a negative number, the negative sign is ignored.

